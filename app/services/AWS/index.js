const upload = require('./uploadFile')
const getUrl = require('./getUrlFile')
const remove = require('./removeFile')
const publicUrl = require('./getPublicUrl')

module.exports = {
  upload,
  getUrl,
  remove,
  publicUrl
}
